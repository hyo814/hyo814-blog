---
title: '[99클럽] 99클럽 코테 스터디 20일차TIL + 힙'
date: '2025-02-14'
tags: [코딩테스트]
draft: false
summary: '비기너 회전초밥 https://www.acmicpc.net/problem/28107'
---

# [99클럽] 99클럽 코테 스터디 20일차TIL + 힙

> 이 글은 [네이버 블로그](https://blog.naver.com/ggamjige8888/223760549159)에 2025년 2월 14일에 올렸던 것을 그대로 옮겨온 것입니다.

- 오늘의 학습 키워드

- 공부한 내용 본인의 언어로 정리하기

- 오늘의 회고

- 어떤 문제가 있었고, 나는 어떤 시도를 했는지

- 어떻게 해결했는지

- 무엇을 새롭게 알았는지

- 내일 학습할 것은 무엇인지

비기너 회전초밥 [https://www.acmicpc.net/problem/28107](https://www.acmicpc.net/problem/28107)

미들러 최소 회의실 개수 [https://www.acmicpc.net/problem/19598](https://www.acmicpc.net/problem/19598)

챌린저 거울 설치 [https://www.acmicpc.net/problem/2151](https://www.acmicpc.net/problem/2151)

```js
const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

let input = [];

rl.on('line', (line) => {
  input.push(line.trim());
}).on('close', () => {
  const [N, M] = input[0].split(' ').map(Number);
  let orders = [];
  let sushiSet = Array.from({ length: N }, () => new Set());

  let idx = 1;
  for (let i = 0; i < N; i++) {
    const [k, ...sushis] = input[idx++].split(' ').map(Number);
    for (let s of sushis) sushiSet[i].add(s);
  }

  const sushiMade = input[idx].split(' ').map(Number);
  let eatenCount = Array(N).fill(0);

  for (let i = 0; i < M; i++) {
    let sushi = sushiMade[i];
    for (let j = 0; j < N; j++) {
      if (sushiSet[j].has(sushi)) {
        eatenCount[j]++;
        sushiSet[j].delete(sushi);
        break;
      }
    }
  }

  console.log(eatenCount.join(' '));
  process.exit();
});
```

필수 해시태그: #99클럽 #코딩테스트준비 #개발자취업 #항해99 #TIL

![image](/static/images/naver/223760549159/01.png)
