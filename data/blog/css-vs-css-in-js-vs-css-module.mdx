---
title: CSS vs CSS-IN-JS VS CSS MODULE
date: '2024-06-04'
tags: [CSS, CSS-IN-JS, CSS MODULE]
draft: false
summary: 'CSS vs CSS-IN-JS VS CSS MODULE'
---

# CSS vs CSS-IN-JS VS CSS MODULE

**목차**
1. CSS
2. CSS-IN-JS
3. CSS MODULE

---

# ✍학습 키워드
1. CSS
2. CSS-IN-JS
3. CSS MODULE

---

# 📝새로 배운 개념

## 개념 1

### CSS의 우선 순위와 계단식 구조

CSS의 우선 순위는 CSS 규칙이 적용될 때 어떤 규칙이 우선적으로 적용되는지를 결정하는 규칙입니다. CSS의 우선 순위는 다음과 같은 요소들에 의해 결정됩니다:

- 인라인 스타일 (Inline styles)
- ID 셀렉터 (ID selectors)
- 클래스, 속성, 의사-클래스 셀렉터 (Class, attribute, and pseudo-class selectors)
- 태그(요소) 셀렉터 (Type selectors)
- 전역 셀렉터 (Universal selectors), 속성 셀렉터 (Attribute selectors), 의사-요소 셀렉터 (Pseudo-element selectors)
- 중요도 (Importance) 표시 (!important)

#### 계단식 구조

##### 1. 우선 순위 (Priority)
CSS 규칙은 스타일 시트에서 나오는 순서에 따라 적용됩니다. 만약 동일한 요소에 대해 여러 규칙이 충돌하는 경우, 나중에 정의된 규칙이 우선합니다.

##### 2. 명시도 (Specificity)
CSS 규칙의 명시도는 각 규칙의 "강도"를 결정하는 요소입니다. 명시도는 다음과 같이 계산됩니다:

- 인라인 스타일: 1000
- ID 셀렉터: 100
- 클래스, 속성, 의사 클래스 셀렉터: 10
- 요소(태그) 셀렉터: 1

##### 3. 원천 (Origin)
CSS 규칙의 원천도 중요합니다. CSS 규칙은 세 가지 원천에서 올 수 있습니다:

- 브라우저 기본 스타일 (User Agent Stylesheet)
- 사용자가 정의한 스타일 (User Stylesheet)
- 개발자가 정의한 스타일 (Author Stylesheet)

#### CSS 상속

CSS 상속은 특정 CSS 속성이 부모 요소에서 자식 요소로 자동으로 전달되는 것을 의미합니다. 이는 CSS가 웹 페이지의 스타일을 쉽게 관리할 수 있도록 도와줍니다.

##### 상속의 기본 개념

- 상속되는 속성: 텍스트 관련 속성들, 예를 들어 color, font-family, font-size, line-height, text-align 등은 상속됩니다.
- 상속되지 않는 속성: 박스 모델 관련 속성들, 예를 들어 margin, padding, border, width, height 등은 상속되지 않습니다.

##### 상속과 우선 순위의 상호작용
상속된 스타일은 특정 요소에 직접 적용된 스타일 규칙보다 우선 순위가 낮습니다. 즉, 상속된 스타일은 해당 요소에 다른 스타일이 적용되지 않았을 때만 적용됩니다.

##### CSS의 계단식 구조

- **중요도(Importance)**: !important 규칙은 모든 다른 선언보다 높은 우선순위를 가집니다.
- **특이성(Specificity)**: 특이성이 높은 선택자는 특이성이 낮은 선택자보다 우선합니다. 예를 들어, ID 선택자는 클래스 선택자보다 특이성이 높고, 클래스 선택자는 태그 선택자보다 특이성이 높습니다.
  - ID 선택자 (#id): 0,1,0
  - 클래스 선택자 (.class), 속성 선택자 ([type="text"]), 가상 클래스 (:hover): 0,0,1
  - 태그 선택자 (div, h1), 가상 요소 (::before): 0,0,0,1
- **소스 순서(Source Order)**: HTML 문서에 나타나는 순서대로 스타일이 적용됩니다. 같은 중요도와 특이성을 가진 규칙이라면, 나중에 선언된 스타일이 우선 적용됩니다.

---

## 개념 2


### CSS 모듈(CSS Modules)

CSS 모듈은 전통적인 CSS 파일을 모듈 단위로 나누어 사용하는 방법입니다. 각 CSS 클래스는 고유의 범위를 가지므로, 전역 클래스 이름 충돌을 피할 수 있습니다.

#### 장점

##### 스코프 제한
CSS 모듈은 클래스 이름을 로컬로 스코프하여 클래스 이름 충돌을 방지합니다. 컴포넌트마다 고유한 클래스 이름이 생성되므로 전역 스타일 충돌을 최소화할 수 있습니다.

##### 가독성 및 유지보수성
컴포넌트 단위로 CSS를 분리하면 코드의 가독성이 높아지고, 유지보수가 용이합니다. 각 컴포넌트에 해당하는 스타일이 모듈화되어 있어, 필요한 스타일만 로드하고 수정할 수 있습니다.

##### 자동 완성 및 타입 지원
CSS 모듈을 사용할 때 타입스크립트와 함께 사용하면, IDE에서 자동 완성과 타입 체크를 지원받을 수 있습니다.

#### 단점

##### 설정 복잡성
CSS 모듈을 설정하려면 Webpack 등의 빌드 도구 설정이 필요합니다. 초기 설정이 복잡할 수 있습니다.

##### 중복 코드
공통 스타일이 여러 컴포넌트에 걸쳐 사용될 경우, 중복된 스타일 코드가 생길 수 있습니다.

#### 사용 예시

##### Button.module.css
```css
.button {
  background-color: blue;
  color: white;
}
```

##### Button.js
```javascript
import styles from './Button.module.css';

const Button = () => {
  return <button className={styles.button}>Click Me</button>;
};
```

#### CSS 모듈의 장점

##### 이름 충돌 방지
각 클래스는 고유하게 이름이 지정되어 충돌을 피할 수 있습니다.

##### 간단한 설정
기존의 CSS를 사용할 수 있어, 기존 프로젝트에 도입하기 쉽습니다.

##### 정적 분석 가능
CSS 파일이 정적이기 때문에 빌드 시 분석이 쉽습니다.

#### 단점

##### 범위 제한
범위를 넘는 스타일 적용이 어렵습니다. (예: 전역 스타일 적용)

##### 파일 관리
많은 컴포넌트가 있는 경우 CSS 파일이 늘어나 관리가 복잡해질 수 있습니다.

##### 동적 스타일링 어려움
동적으로 스타일을 변경하는 것이 어렵습니다.

#### 사용 예시

##### ExampleComponent.module.css
```css
.title {
  color: blue;
}
```

##### ExampleComponent.jsx
```javascript
import styles from './ExampleComponent.module.css';

function ExampleComponent() {
  return <h1 className={styles.title}>Hello, World!</h1>;
}
```


## 개념 3
CSS-IN-JS로 STYLED 컴포넌트와 EMOTION

### CSS-in-JS

CSS-in-JS는 JavaScript 안에 CSS를 작성하는 방법으로, 스타일을 컴포넌트와 밀접하게 결합할 수 있습니다. 대표적인 라이브러리로는 styled-components와 Emotion이 있습니다.

#### 장점

- **동적 스타일링**: props나 state에 따라 스타일을 동적으로 변경할 수 있습니다.
- **캡슐화**: 스타일이 컴포넌트 내부에 캡슐화되어, 재사용성과 관리가 용이합니다.
- **JS 기능 사용 가능**: JavaScript의 모든 기능을 스타일링에 활용할 수 있습니다.

#### 단점

- **성능 문제**: 런타임에 스타일을 생성하므로, 성능 이슈가 발생할 수 있습니다.
- **빌드 설정 복잡**: Babel이나 Webpack 설정이 복잡해질 수 있습니다.
- **초기 학습 비용**: 기존 CSS와 다르게 새로운 문법과 사용법을 배워야 합니다.

#### 예시

##### ExampleComponent.jsx
```javascript
import styled from 'styled-components';

const Title = styled.h1`
  color: blue;
`;

function ExampleComponent() {
  return <Title>Hello, World!</Title>;
}
```

### Styled Components와 Emotion 비교

#### Styled Components

**장점:**

- **간편한 사용**: 스타일을 컴포넌트 안에 작성하기 쉽고 직관적입니다.
- **CSS 구문 지원**: 기존 CSS 구문을 그대로 사용할 수 있어 학습 곡선이 낮습니다.
- **테마 지원**: 테마를 정의하고 쉽게 적용할 수 있습니다.

**단점:**

- **런타임 오버헤드**: 스타일이 런타임에 생성되므로, 성능에 영향을 줄 수 있습니다.
- **패키지 크기**: 비교적 큰 패키지 크기를 가질 수 있습니다.

#### Emotion

**장점:**

- **높은 성능**: 바벨 플러그인을 사용하여 빌드 타임에 스타일을 생성함으로써 성능을 최적화할 수 있습니다.
- **유연성**: css 함수와 styled 함수 둘 다 사용할 수 있어 유연합니다.
- **경량화**: 작은 패키지 크기를 가집니다.

**단점:**

- **설정 복잡성**: 최적의 성능을 위해 바벨 플러그인 설정이 필요합니다.
- **학습 필요**: 다양한 사용법이 있어 학습이 필요할 수 있습니다.

#### 예시

##### Styled Components
```javascript
import styled from 'styled-components';

const Button = styled.button`
  background: blue;
  color: white;
  padding: 10px;
`;

function App() {
  return <Button>Click Me</Button>;
}
```

##### Emotion
```javascript
/** @jsxImportSource @emotion/react */
import { css } from '@emotion/react';
import styled from '@emotion/styled';

const buttonStyle = css`
  background: blue;
  color: white;
  padding: 10px;
`;

const Button = styled.button`
  ${buttonStyle}
`;

function App() {
  return <Button>Click Me</Button>;
}
```

---

# ✨느낀 점&자기 선언

학습하며 깨달은 점들을 정리해보세요. 단순히 느낀 점도 좋고, 더 궁금한 점이나 부족한 점도 좋아요. 학습한 내용을 바탕으로 어떻게 개선할지가 중요하니까요. 오늘의 피드백으로 내일 더 성장해봐요.


# Styled Components, Emotion, CSS, CSS Module 비교

| **특징**             | **Styled Components**                                                                                  | **Emotion**                                                                                       | **CSS**                                      | **CSS Module**                                                                                   |
|----------------------|--------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------|----------------------------------------------|-------------------------------------------------------------------------------------------------|
| **동적 스타일링**    | props와 state를 사용하여 동적 스타일링 가능                                                              | props와 state를 사용하여 동적 스타일링 가능                                                         | 제한적, JS를 사용하여 동적 스타일링 필요        | 불가능                                                                                           |
| **캡슐화**           | 컴포넌트 내부에 스타일 캡슐화                                                                          | 컴포넌트 내부에 스타일 캡슐화                                                                       | 전역 스코프                                    | 로컬 스코프, 클래스명 고유화                                                                       |
| **JS 기능 사용**     | JavaScript의 모든 기능을 스타일링에 활용 가능                                                          | JavaScript의 모든 기능을 스타일링에 활용 가능                                                       | 불가능                                        | 불가능                                                                                           |
| **성능**             | 런타임 스타일 생성으로 성능 저하 발생 가능                                                              | 바벨 플러그인을 사용하여 빌드 타임에 스타일 생성 가능                                               | 성능 우수                                      | 성능 우수                                                                                         |
| **빌드 설정**        | Babel, Webpack 설정 필요                                                                              | Babel, Webpack 설정 필요                                                                           | 간단                                          | 간단                                                                                             |
| **학습 곡선**        | 낮음                                                                                                   | 중간                                                                                              | 낮음                                           | 낮음                                                                                              |
| **패키지 크기**      | 비교적 큼                                                                                               | 경량                                                                                              | 없음                                           | 없음                                                                                              |
| **전역 스타일 적용** | 불가능                                                                                                | 불가능                                                                                            | 가능                                           | 제한적                                                                                             |
| **정적 분석**        | 불가능                                                                                                | 불가능                                                                                            | 가능                                           | 가능                                                                                              |
| **설정 복잡성**      | 중간                                                                                                   | 중간                                                                                              | 낮음                                           | 낮음                                                                                              |
| **사용 예시**        | ```javascript<br>import styled from 'styled-components';<br>const Button = styled.button`background: blue; color: white; padding: 10px;`;<br>function App() {<br> return <Button>Click Me</Button>;<br>}``` | ```javascript<br>/** @jsxImportSource @emotion/react */<br>import { css } from '@emotion/react';<br>import styled from '@emotion/styled';<br>const buttonStyle = css`background: blue; color: white; padding: 10px;`;<br>const Button = styled.button`${buttonStyle}`;<br>function App() {<br> return <Button>Click Me</Button>;<br>}``` | ```css<br>.button { background: blue; color: white; padding: 10px; }``` | ```javascript<br>// Button.module.css<br>.button { background: blue; color: white; padding: 10px; }<br>// Button.js<br>import styles from './Button.module.css';<br>const Button = () => {<br> return <button className={styles.button}>Click Me</button>;<br>};``` |

---

# 🦋활용 사례

## 활발한 사용 분야

학습한 개념이 활발하게 사용되는 분야가 있다면 무엇인가요? 해당 분야의 특징 및 사용 특징이 있다면 작성해보세요.

# Styled Components, Emotion, CSS, CSS Module 활용 비교

| **기술**             | **주로 활용하는 경우**                                                                                                                                    |
|----------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Styled Components** | - 동적 스타일링이 필요할 때<br>- 컴포넌트 단위로 스타일을 관리하고 싶을 때<br>- JavaScript의 기능을 스타일링에 활용하고 싶을 때<br>- 테마를 쉽게 적용하고 싶을 때 |
| **Emotion**           | - 성능 최적화가 중요할 때<br>- 동적 스타일링과 테마 적용이 필요할 때<br>- 작은 패키지 크기가 중요한 프로젝트일 때<br>- 유연한 스타일링 방법이 필요할 때        |
| **CSS**               | - 간단한 스타일링이 필요할 때<br>- 프로젝트가 작고 복잡하지 않을 때<br>- 기존의 전통적인 방식으로 스타일링을 관리하고 싶을 때                               |
| **CSS Module**        | - 클래스 이름 충돌을 방지하고 싶을 때<br>- 컴포넌트 단위로 스타일을 관리하고 싶을 때<br>- 정적 분석과 간단한 설정을 원할 때                                   |


## 예시 코드

### Styled Components
```javascript
import styled from 'styled-components';

const Button = styled.button`
  background: blue;
  color: white;
  padding: 10px;
`;

function App() {
  return <Button>Click Me</Button>;
}
```

### Emotion
```javascript
/** @jsxImportSource @emotion/react */
import { css } from '@emotion/react';
import styled from '@emotion/styled';

const buttonStyle = css`
  background: blue;
  color: white;
  padding: 10px;
`;

const Button = styled.button`
  ${buttonStyle}
`;

function App() {
  return <Button>Click Me</Button>;
}
```

### CSS
```css
/* styles.css */
.button {
  background: blue;
  color: white;
  padding: 10px;
}
```

```html
<!-- index.html -->
<link rel="stylesheet" href="styles.css">
<button class="button">Click Me</button>
```

### CSS Module
```css
/* Button.module.css */
.button {
  background: blue;
  color: white;
  padding: 10px;
}
```

```javascript
// Button.js
import styles from './Button.module.css';

function Button() {
  return <button className={styles.button}>Click Me</button>;
}
```

---

# 🔗레퍼런스

## 참고 강의/글
- https://emotion.sh/docs/introduction
- https://styled-components.com/docs
- https://github.com/css-modules/css-modules
- https://www.tcpschool.com/react/react_styling_cssmodule

## 읽을 예정

- https://www.freecodecamp.org/news/how-to-use-css-overview-in-chrome-developer-tools/
- https://developer.mozilla.org/ko/docs/Learn/CSS
