---
title: '[99클럽] 99클럽 코테 스터디 10일차 + 스택과 큐'
date: '2025-04-11'
tags: [코딩테스트]
draft: false
summary: 'const rl = readline.createInterface({ input: process.stdin, output: process.stdout, });'
---

# [99클럽] 99클럽 코테 스터디 10일차 + 스택과 큐

> 이 글은 [네이버 블로그](https://blog.naver.com/ggamjige8888/223829395421)에 2025년 4월 11일에 올렸던 것을 그대로 옮겨온 것입니다.

- 오늘의 학습 키워드

- 공부한 내용 본인의 언어로 정리하기

- 오늘의 회고

- 어떤 문제가 있었고, 나는 어떤 시도를 했는지

- 어떻게 해결했는지

- 무엇을 새롭게 알았는지

- 내일 학습할 것은 무엇인지

```js
const readline = require("readline");

const rl = readline.createInterface({
input: process.stdin,
output: process.stdout,
});

let input = [];

rl.on("line", (line) => {
input.push(line);
}).on("close", () => {
const n = parseInt(input[0]);
const xMap = new Map();
const yMap = new Map();

for (let i = 1; i <= n; i++) {
const [x, y] = input[i].split(" ").map(Number);

xMap.set(x, (xMap.get(x) || 0) + 1);
yMap.set(y, (yMap.get(y) || 0) + 1);
}

let count = 0;

for (const v of xMap.values()) {
if (v >= 2) count++;
}

for (const v of yMap.values()) {
if (v >= 2) count++;
}

console.log(count);
});
```

필수 해시태그: #99클럽 #코딩테스트준비 #개발자취업 #항해99 #TIL

![image](/static/images/naver/223829395421/01.png)
