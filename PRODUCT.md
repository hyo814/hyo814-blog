# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1순위: 채용 담당자·팀장.** 지원 서류(원티드 프로필, 이력서)와 함께 이 사이트를 열어보는 상황. 짧은 시간에 "무엇을 할 수 있는 사람인지"와 "주장에 근거가 있는지"를 판단하려 한다. 홈·About·Projects·Timeline이 이 독자를 위한 면이다.

**2순위: 같은 문제를 검색해서 들어온 개발자.** 대부분 구글에서 개별 글로 직접 진입한다. 사이트 전체를 돌아보지 않고 한 편만 읽고 떠나는 것이 정상 경로다. 블로그 본문 면이 이 독자를 위한 면이다.

두 독자가 겹치지 않으므로 진입 경로도 다르다. 포트폴리오 면이 우선순위를 갖되, 글 면의 읽기 경험을 희생하지 않는다.

## Product Purpose

풀스택 개발자 임효진(Hyo814)의 **포트폴리오 겸 기술 블로그**. 이력서에 한 줄로 적힌 성과의 근거를 실제 기록으로 열어 보이는 것이 목적이다.

성공은 "글이 많다"가 아니라, 채용 담당자가 **주장 → 근거 글**로 한 번에 이동해 확인할 수 있는 상태다. 경력기술서 3편이 개별 회고 글로 링크되어 있는 현재 구조가 이 목적의 핵심 자산이다.

## Positioning

**틀렸던 것을 적어둔 블로그.** 다른 개발자 블로그가 성공 사례만 남기는 자리에, 이 블로그는 뒤집힌 결정과 철회한 기능, 근거가 남지 않은 수치를 명시한다.

- "그리고 내가 만든 건 useState가 아니었다"
- "이 글이 틀렸던 것: 클래스 순서는 우선순위가 아니다"
- "일정 데이터 동기화 문제 해결하기 — 그리고 이 해결책은 나중에 뒤집혔다"
- SDMS 경력기술서 §9 "도입했다가 철회한 것 — 실패의 기록"
- 산업 AI 경력기술서: 유리한 수치(40% 단축)를 적고도 "측정 방법이 남아 있지 않다"고 스스로 깎음

이 정직성은 흉내 낼 수는 있어도 소급해서 만들어낼 수 없다. 디자인은 이 성질을 덮지 말고 드러내야 한다.

## Operating Context

- 채용 담당자는 **모바일에서 먼저** 열어보는 경우가 많다(원티드 앱 경유).
- 검색 유입 독자는 개별 글 URL로 직접 진입하며 헤더·홈을 거치지 않는다.
- 글 본문에는 표, 코드 블록, 수치 비교(전/후), 다른 글로 가는 내부 링크가 반복적으로 등장한다. 이 네 요소가 본문 디자인의 실제 부하다.

## Capabilities and Constraints

- Next.js 15 App Router + Contentlayer2 + Tailwind. `tailwind-nextjs-starter-blog` v2.2.0 기반이며 테마는 기본값 그대로다(교체 대상).
- 글 125편. 슬러그가 한글이며 파일명이 곧 URL이다. **기존 URL은 깨뜨릴 수 없다.**
- 다크모드는 `next-themes` 기반 `ThemeSwitch`로 이미 동작 중.
- 태그 페이지, 검색, RSS, sitemap이 빌드 시 생성된다(231 페이지).
- `components/Header.tsx`가 존재하지 않는 `@/data/logo.svg`를 import 중이다. 미사용이라 트리셰이킹으로 빌드는 통과하지만, 렌더링하는 순간 깨진다.

## Brand Commitments

**반드시 유지 (사용자 확인):**
- **Hyo814** 핸들명 — 헤더 타이틀, GitHub, 사이트명
- **한국어 전용** — 영문 병기 없음
- **다크모드 지원** — 모든 색 결정은 라이트/다크 양쪽에서 성립해야 함

**교체 대상 (사용자 지시):**
- `public/static/images/avatar.png` — 실물 증명사진. 제거.
- `public/static/images/logo.png` — 커비 시리즈 와들디 피규어 사진. `siteLogo`·`socialBanner`로 쓰이는데 타인 저작물이라 소셜 공유 이미지로 부적합. 제거.
- `public/static/favicons/*` — AI 생성 판다 일러스트. 제거.

**미확정:** 실명(임효진) 노출은 "반드시 유지" 항목으로 선택되지 않았다. 현재 About·Professional Summary에 실명이 있으며, 명시적 지시가 없으므로 **현 상태를 보존한다.** 제거를 원하면 별도 확인이 필요하다.

## Evidence on Hand

**실재하는 것:**
- 글 125편(`data/blog/`). 2025-04 이후 15개월에 약 70편이 집중.
- 경력기술서 3편 — 산업 AI 판독 시스템(2021–2022), 숙박 큐레이션 플랫폼(2022–2024), SDMS(2025–현재). 세 편 모두 `한눈에 보기` 표 → 번호 섹션 → `돌아보면` 구조로 통일.
- 검증된 단독 성과 수치: 메타클래스 목록 쿼리 300건 기준 **908쿼리·1.6s → 4쿼리·180ms**, 목업 시드 **수작업 49개 → 엑셀 기반 478개**, 트리 JS 3개 → 공통 유틸 1개(333줄).
- 프로젝트 스크린샷: `triptune/`, `zigzzang/`, `fancuk/`, `escape/`, `히투지투/`, `stayfolio/`, `deepnoid/`.
- 수료증 이미지 6종.
- Timeline 이력 데이터 22건(`components/TimeLineComponent.tsx`).

**없는 것 — 지어내지 말 것:**
- 실무 3사(산업 AI·숙박·SDMS)의 공개 가능한 UI 스크린샷은 확보되지 않았다. Projects 카드에 이미지가 없는 이유다.
- 숙박 플랫폼의 캠페인 수치(앱 설치율 600% 등)는 **전사 캠페인 지표**이며 본문에 담당 범위가 구분되어 표기돼 있다. 개인 단독 성과로 승격시키면 안 된다.
- 산업 AI 시기(2021–2022) 성과의 측정 방법은 남아 있지 않다.
- 방문자 수, 구독자 수, 추천사 등 사이트 자체의 지표는 존재하지 않는다.

## Product Principles

1. **주장 옆에는 근거로 가는 문이 있다.** 수치를 보여줄 때 그 수치를 측정한 글로 이동할 수 있어야 한다. 이 사이트의 구조적 강점이며 디자인이 이를 약화시켜선 안 된다.
2. **정직성이 1급 콘텐츠다.** "틀렸다", "뒤집혔다", "근거가 없어 적지 않는다"는 문장은 숨길 결함이 아니라 전면에 둘 자산이다.
3. **두 독자의 진입 경로를 모두 존중한다.** 포트폴리오 면이 우선이되, 검색으로 들어온 사람이 헤더를 거치지 않고 본문만 읽어도 완결되어야 한다.
4. **범위를 부풀리지 않는다.** 전사 성과와 개인 기여를 구분해 표기하는 현재 원칙을 UI에서도 유지한다.
5. **URL은 유산이다.** 125편의 한글 슬러그는 검색 자산이므로 어떤 개편에서도 보존한다.

## Accessibility & Inclusion

- 다크/라이트 양 모드에서 본문 대비가 성립해야 한다(현재 Timeline 카드가 다크모드 미대응이었고 수정됨).
- 본문에 표와 넓은 코드 블록이 자주 등장하므로 모바일에서 가로 스크롤은 해당 요소 내부로 가둬야 한다.
- 채용 담당자의 모바일 우선 열람을 전제로 한다.
