# Design

<!-- impeccable:design-schema 1 -->

## Direction contract

**THESIS** — 이 사이트는 "주장 옆에 근거로 가는 문이 있다"는 한 가지를 소유한다. 개발자 블로그가 늘 출고하는 배치(카드 그리드로 깔린 글 목록 + 큰 이름이 박힌 히어로)를 거부하고, **측정 기록(전 → 후)** 자체를 첫 화면의 주인공으로 세운다.

**OWN-WORLD** — 종이(warm paper)와 점토(clay)의 두 물질. 배경은 순백이 아닌 따뜻한 미색, 강조는 분홍이 아닌 구운 점토색이며, 이 점토색은 링크에 흩뿌려지는 악센트가 아니라 **면 전체를 소유한다**(홈 지표 밴드, About 모노그램, Timeline 레일, 푸터). 다크 모드는 차가운 회색이 아니라 따뜻한 먹색(#141413)이다. 모서리는 거의 직각(2–4px), 그림자는 오프셋과 번짐을 가진 실물 그림자만 쓴다. 본문은 명조 계열 표제 + 고딕 본문의 편집물 조합.

**STORY** — 채용 담당자는 첫 화면에서 "프런트 5년 → 풀스택으로 확장한 사람"과 "908쿼리 → 4쿼리" 같은 검증된 수치를 동시에 본다. 수치를 누르면 그 수치를 측정한 글로 이동한다. 읽고 나면 "이 사람은 근거를 남기는 사람"이라고 이해한 상태로 Projects 또는 경력기술서로 넘어간다.

**FIRST VIEWPORT** — 상단에 정체를 한 문장으로 박은 표제(명조, 최대 3.5rem), 그 아래 한 줄 요약. 바로 이어서 **점토색이 면을 채운 지표 밴드**가 가로로 놓이고 그 안에 검증된 수치 3개가 전/후 형태로 들어간다. 각 수치는 근거 글로 가는 링크다. 그 아래부터 최신 글 목록. 주 동선(경력기술서 보기)은 표제 바로 아래에 놓인다.

**FORM** — 브리프가 세계를 고정했다(Anthropic 디자인 언어). 참조 문서 규칙에 따라 사용자가 핀한 방향은 롤보다 우선하므로 컨셉 룰렛을 돌리지 않았다. 고정된 세계의 **가장 부드러운 렌디션이 아니라 채도가 살아 있는 물질 범위**로 렌더한다 — 미색 바탕에 악센트만 찍는 기본형을 피하고, 점토색이 면을 소유하게 한다.

## Platform

web

## Color

전략: **Committed.** 점토색이 포트폴리오 면(홈·About·Timeline·푸터)에서 30–60%의 면적을 소유한다. 읽기 면(글 목록·본문)은 Restrained로 되돌아가 본문 가독성을 지킨다.

물리적 장면: *채용 담당자가 낮에 사무실 모니터로, 혹은 이동 중 폰으로 지원 서류 사이에서 이 사이트를 연다.* → **라이트가 기본**, 다크는 완전 대응.

### 토큰

| 역할 | 라이트 | 다크 |
|---|---|---|
| `paper` (바탕) | `#FAF8F4` | `#141413` |
| `surface` (올라온 면) | `#F0EDE6` | `#1F1E1D` |
| `line` (경계) | `#E2DDD2` | `#2E2C2A` |
| `ink` (본문) | `#191817` | `#F0EEE6` |
| `muted` (보조) | `#6B6862` | `#A29E96` |
| `clay` (텍스트·악센트) | `#A64B2E` | `#E08B6B` |
| `clayfield` (색이 소유하는 면) | `#A64B2E` | `#8F4026` |

`clay`와 `clayfield`를 나눈 이유: 다크 모드에서 `clay`는 밝아져야 먹색 위에서 읽히고(7.09:1), 면 배경은 반대로 어두워져야 그 위의 아이보리 글자가 읽힌다(6.48:1). 하나의 토큰으로는 두 역할이 동시에 성립하지 않는다.

초안의 `#C15F3C`는 포기했다. 아이보리 텍스트 대비가 3.82:1, paper 위 링크 대비가 3.98:1로 **둘 다 AA 미달**이었다. `#A64B2E`는 각각 5.18:1 / 5.40:1이고, 더 짙은 북클로스 쪽이라 "가장 부드러운 렌디션을 피한다"는 이 문서의 방침과도 맞는다.

측정된 대비 (13쌍 전부 AA 통과): 본문 16.72 / 보조 5.24 / 링크 5.40 (라이트), 본문 15.87 / 보조 6.91 / 링크 7.09 (다크), 밴드 위 텍스트 4.51–6.48.

`clay` 50–950 스케일을 Tailwind `primary`로 등록해 기존 유틸리티(`text-primary-500`)가 그대로 동작하게 한다. 스타터의 `colors.pink` 매핑은 제거했고, Tailwind 기본 `gray`는 `colors.stone`(웜 뉴트럴)으로 갈아끼워 손대지 않은 화면까지 같은 온도로 따라오게 했다.

색 있는 면 위의 보조 텍스트는 회색이 아니라 **그 면의 색조에서 틴트**한다(점토 면 위에서는 `clay-100`).

## Typography

| 역할 | 서체 | 비고 |
|---|---|---|
| 표제 (display) | **Gowun Batang** | 한글 명조. 편집물의 표제 레지스터를 담당. 400/700만 존재하므로 굵기가 아니라 크기로 위계를 만든다. |
| 본문·UI | **Noto Sans KR** | 한글·라틴이 한 설계 안에 있어 혼용 줄에서 높이가 어긋나지 않는다. 이 블로그는 한 문장 안에 `Django`·`prefetch` 같은 라틴이 상시 섞인다. |
| 코드 | 시스템 모노 스택 | 새 서체를 붙이지 않는다. |

- 본문 measure 65–75ch, display 최대 6rem, 트래킹 하한 -0.04em.
- 표제 위 여백 > 표제 아래 여백.
- **모노스페이스는 코드·데이터·측정값에만 쓴다.** "기술적으로 보이려고" 쓰지 않는다.
- `Space Grotesk`(스타터 기본값)는 제거한다.

## Components

- **모서리:** 2–4px. 완전한 pill은 태그에만 허용.
- **그림자:** 오프셋 + 번짐을 가진 것만. 오프셋 0의 색 후광은 금지.
- **카드:** 아이콘+제목+본문이 같은 크기로 반복되는 카드 그리드를 페이지 구조로 쓰지 않는다. Projects는 실무/사이드가 시각적으로 구분되는 목록형.
- **중첩 카드 금지.**
- **좌측 컬러 보더는 1px까지.**
- **Monogram:** `H814`를 점토 면 위 아이보리 글자로. SVG 컴포넌트 하나가 About 아바타·헤더 마크·파비콘·소셜 배너를 모두 담당한다. 외부 이미지 파일에 의존하지 않으므로 다크 모드가 자동으로 따라온다.

## Motion

한 번의 저작된 순간만 둔다: **홈 지표 밴드의 수치가 전 → 후로 넘어가는 전환.** 이미 보이는 상태에서 시작해 지수 ease-out으로 정착한다. 섹션마다 반복되는 동일 등장 애니메이션은 쓰지 않는다. `prefers-reduced-motion`에서 최종 상태로 즉시 고정.

## Preserved (변경 금지)

- 한글 슬러그 URL 125편 — 검색 자산.
- 글 본문 내용, 프론트매터, 태그 문자열.
- 다크모드 토글 동작(`next-themes`).
- `Hyo814` 핸들명, 한국어 전용 표기.
- RSS·sitemap·검색 인덱스 생성 경로.

## Prohibitions

세계 자체가 쓰는 장치를 금지하지 않도록 확인함 — 점토색 면, 따뜻한 미색, 명조 표제는 이 세계의 고유 재료이므로 허용된다. 금지 항목은 다음뿐이다.

- 그라디언트 텍스트 (강조는 크기·굵기로).
- 장식용 glass/blur.
- 히어로 지표 템플릿의 관성적 사용 — 홈의 지표 밴드는 **각 수치가 근거 글로 가는 링크일 때만** 성립한다. 링크 없는 큰 숫자는 이 세계에서 금지다.
- 모든 섹션 위의 대문자 트래킹 eyebrow.
- 정보가 없는 섹션 번호(01/02/03).
